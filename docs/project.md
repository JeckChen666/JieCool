# JieCool 项目快速上手指南

## 项目简介

JieCool 是一个个人网站项目，采用前后端分离架构。

**技术栈**：
- 前端：Next.js 14.2.15 + TypeScript + Arco Design 2.66.5
- 后端：GoFrame v2.9.3 + Go 1.23.0 + PostgreSQL 18

## 环境准备

### 必需软件
- **Node.js**: 18+ 
- **Go**: 1.23.0+
- **PostgreSQL**: 18
- **Git**: 最新版本

### 数据库准备
```sql
-- 创建数据库
CREATE DATABASE JieCool;

-- 创建用户（可选）
CREATE USER admin WITH PASSWORD '123456';
GRANT ALL PRIVILEGES ON DATABASE JieCool TO admin;
```

**连接信息**：
- 主机：127.0.0.1
- 端口：5432
- 数据库：JieCool
- 用户：admin
- 密码：123456

## 项目启动

### 1. 克隆项目
```bash
git clone <项目地址>
cd JieCool
```

### 2. 启动后端
```bash
cd server

# ⚠️ 重要：本项目使用 GoFrame CLI 命令
# 先安装 GoFrame CLI（只需要安装一次）
go install github.com/gogf/gf/cmd/gf/v2@latest

# 启动开发服务器（推荐，支持热重载）
gf run main.go

# 或者使用 Makefile（会自动安装 gf CLI）
make run
```

**验证后端启动**：
- 访问：http://localhost:8080/swagger
- 看到 API 文档页面即启动成功

**❌ 不要使用传统方式**：
```bash
# ❌ 错误：不支持热重载，不推荐
go run main.go
```

### 3. 启动前端
```bash
cd front-web

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**验证前端启动**：
- 访问：http://localhost:3000
- 看到项目页面即启动成功

## 项目结构

### 整体结构
```
JieCool/
├── docs/              # 项目文档
├── front-web/         # 前端项目
├── server/           # 后端项目
├── CLAUDE.md         # Claude AI 指导文档
└── README.md         # 项目说明
```

### 前端结构 (`front-web/`)
```
front-web/
├── src/app/          # 页面路由（Next.js App Router）
├── src/components/   # React 组件
├── src/lib/         # 工具库和API客户端
├── public/          # 静态资源
├── package.json     # 依赖配置
└── tailwind.config.js # 样式配置
```

**重要文件**：
- `src/app/layout.tsx` - 全局布局
- `src/app/test/page.tsx` - 路由测试页面
- `src/lib/alova.ts` - HTTP 客户端配置

### 后端结构 (`server/`)
```
server/
├── api/             # API 接口定义
├── internal/        # 业务代码
│   ├── controller/  # 控制器（处理HTTP请求）
│   ├── service/     # 业务逻辑
│   ├── dao/         # 数据访问
│   └── model/       # 数据模型
├── manifest/config/ # 配置文件
├── hack/           # 构建脚本
├── main.go         # 程序入口
└── Makefile        # 构建命令
```

**重要文件**：
- `main.go` - 程序入口
- `manifest/config/config.yaml` - 应用配置
- `Makefile` - 构建和开发命令

## 常用开发命令

### 后端命令
```bash
cd server

# ⚠️ 重要：所有后端开发都使用 GoFrame CLI 命令

# 开发服务器（热重载）
gf run main.go

# 代码生成命令
gf gen ctrl    # 生成控制器
gf gen dao     # 生成数据访问层
gf gen service # 生成服务接口
gf gen enums   # 生成枚举

# 构建项目
gf build

# 或者使用 Makefile（会自动执行对应的 gf 命令）
make run       # 等同于 gf run main.go
make ctrl      # 等同于 gf gen ctrl
make dao       # 等同于 gf gen dao
make service   # 等同于 gf gen service
make build     # 等同于 gf build
```

**📋 GoFrame CLI 命令速查**：
- `gf run main.go` - 启动开发服务器
- `gf gen ctrl` - 根据API定义生成控制器
- `gf gen dao` - 根据数据库表生成DAO代码
- `gf gen service` - 从logic生成服务接口
- `gf build` - 构建项目

### 前端命令
```bash
cd front-web

# 开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm run start

# 代码检查
npm run lint
```

## 核心功能模块

| 模块 | 功能 | 入口文件 |
|------|------|----------|
| 文件管理 | 文件上传下载、缩略图、MD5校验 | `src/app/file-management/page.tsx` |
| 每日一句 | 英语句子展示、音频播放 | `src/app/page.tsx` |
| 用户认证 | 登录、Token管理 | `src/app/login/page.tsx` |
| 访问统计 | 用户行为记录 | `src/components/VisitTracker.tsx` |
| 动态配置 | 运行时配置管理 | `src/app/admin/config/page.tsx` |
| 微博模块 | 微博内容管理 | `src/app/weibo/page.tsx` |

## 学习资源

### 必读文档
1. **API 接口** → 查看 `docs/api/README.md` 了解所有接口
2. **数据库设计** → 查看 `docs/db/db.md` 了解数据表结构
3. **开发规范** → 查看 `CLAUDE.md` 了解代码规范和约定

### 功能模块文档
- **认证模块** → `docs/api/auth.md`
- **文件管理** → `docs/api/file_api.md`
- **每日一句** → `docs/api/daily.md`

### 技术文档
- **前端框架** → `docs/front-web/nextjs_doc/`
- **UI 组件库** → `docs/front-web/arco-design_doc/`
- **后端框架** → `docs/server/goframe_doc/`

## 开发工作流

### 新功能开发流程
1. **后端**：在 `server/api/` 定义接口 → `gf gen ctrl` → 实现业务逻辑
2. **前端**：开发页面组件 → 更新路由登记（`src/app/test/page.tsx`）
3. **测试**：前后端联调 → 更新文档

**🔧 后端开发工作流**：
```bash
# 1. 在 server/api/ 中定义接口
# 2. 生成控制器代码
gf gen ctrl

# 3. 如果涉及新数据表
gf gen dao

# 4. 生成服务接口
gf gen service

# 5. 启动开发服务器（支持热重载）
gf run main.go
```

### 问题排查
1. **后端启动失败** → 检查数据库连接和端口占用
2. **前端API调用失败** → 检查后端服务状态和环境变量
3. **gf 命令未找到** → 安装 GoFrame CLI：
   ```bash
   go install github.com/gogf/gf/cmd/gf/v2@latest
   ```
4. **代码生成失败** → 确保GoFrame CLI已安装且配置正确

**⚠️ 重要提醒**：
- **必须使用** `gf` 命令进行后端开发
- **不要使用** `go run main.go`（不支持热重载）
- **推荐使用** `gf run main.go`（支持代码变更自动重启）

## 获取帮助

- **项目架构** → 查看 `docs/project.md`（本文档）
- **详细API文档** → 查看 `docs/api/` 目录
- **执行记录和问题修复** → 查看 `docs/execute/` 目录
- **依赖管理** → 查看 `docs/depend/index.md`

---

**提示**：本文档提供项目概览和快速上手指南。详细信息请参考对应的专业文档。