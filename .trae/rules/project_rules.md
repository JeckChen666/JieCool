在我没有特别声明的情况下，你默认执行以下行为：
- 你需要使用简体中文回答我的所有问题。
- 代码中必须有详细的注释。
- 代码中所有的注释都必须使用中文。
- 你不需要写测试用例。
- 如果需要引入新的依赖库，你需要向我说明其功能、作用、可能遇到的问题、解决方案等。只有在我同意之后，你才可以引入。
- 积极从项目的docs目录下获取信息。docs目录下会包含项目使用的框架文档、数据库文档、程序设计文档等。
- 在代码设计的时候，你需要考虑代码的可维护性、可扩展性、性能等因素。你需要根据项目的需求和约束，选择合适的设计模式、架构模式、数据库设计等。
- 在前端代码设计的时候需要考虑界面风格、用户交互、响应式设计等因素。你需要根据项目的需求和约束，选择合适的前端框架、组件库、样式库等。可以适当的为前端代码添加一些交互效果、动画等，提升用户体验。
- 使用GoFrame创建新接口的时候，必须使用GoFrame的代码生成命令，通过其生成后的结果，你才能再进行修改。
- 前端调用后端的api的接口时，参数和响应必须使用ts的泛型，并且基于相应的注释。
- 必须在每次对话后，如果对于api接口的定义产生了变化，你需要及时维护docs\api目录，保持api文档的最新性和准确性。每次有新的api接口定义的时候，你需要及时更新api文档，说明相关的接口路径、请求方法、请求参数、响应参数。
- 必须在每次对话后，如果有新的需求，你需要及时维护docs\execute目录，保持文档的最新性和准确性。每次有新的需求的时候，你需要参考是否有相关的执行文档。如果没有，你需要及时创建，并且说明相关的设计逻辑、实现步骤、可能遇到的问题、解决方案等。
- 必须在每次对话后，如果有新的数据库表、字段、索引等变化，你需要及时维护docs\db目录，保持数据库文档的最新性和准确性。每次有新的数据库表、字段、索引等变化的时候，你需要及时更新数据库文档。
- 必须在每次对话后，如果有新的依赖库引入，你需要及时维护docs\depend目录，保持依赖文档的最新性和准确性。每次有新的依赖库引入的时候，你需要及时更新依赖文档，说明相关版本号，主要作用等。
- 必须在每次对话后，如果有新的需求、功能、模块等变化，你需要及时维护docs\project.md文件，保持项目文档的最新性和准确性。每次有新的需求、功能、模块等变化的时候，你需要及时更新项目文档。该文档的结构为：
    - 项目介绍：项目的背景、目标、功能等。
    - 技术栈：项目使用的技术框架、库、工具等。
    - 项目结构：项目的目录结构、主要模块、功能组件等。
    - 部署说明：项目的部署环境、依赖配置、部署步骤等。
    - 注意事项：项目的注意事项、常见问题、解决方案等。
注意，这个文档只需要你做简要的说明，不需要详细的实现步骤。
